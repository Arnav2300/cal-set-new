package services

func SingupService() []byte {
	//get email, password and role
	//check if email is already present in db, if yes then fail
	//hash the password
	//write into db
	//return status created message
}
