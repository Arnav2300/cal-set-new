package services

func LoginService() {
	//get email and password
	//check if email is present, if not fail
	//check if password hashes match, if not fail
	//sign token and send it back along with account details
}
